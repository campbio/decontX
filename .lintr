linters: linters_with_defaults(
    line_length_linter(80),
    object_name_linter = NULL,
    object_length_linter = NULL,
    # object_usage_linter needs the built package namespace to resolve the
    # package's own internal symbols. The lint CI job installs only lintr
    # (no package build), so it cannot load them and flags every internal
    # function/global. That coverage (undefined globals, unused locals) is
    # already provided by R CMD check's "checking R code for possible
    # problems" with the package loaded, so disable it here.
    object_usage_linter = NULL
  )
exclusions: list(
    # Generated by Rcpp / rstantools — never hand-styled.
    "R/RcppExports.R",
    "R/stanmodels.R",
    # Mirrors helpers in the celda package; kept byte-aligned with celda
    # to minimise cross-package divergence (see AGENTS.md), so it is not
    # restyled to decontX's conventions.
    "R/celda_functions.R",
    # Vignettes are prose + example code, knit locally and not exercised
    # by R CMD check; their chunk style is not part of the blocking lint.
    "vignettes/decontX.Rmd",
    "vignettes/decontPro.Rmd"
  )
encoding: "UTF-8"
